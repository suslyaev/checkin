# Интерактивная таблица гостей мероприятия

## Описание

Модуль добавляет интерактивную таблицу для работы со списком гостей мероприятия в стиле Google Sheets прямо в Django админке.

### Возможности:

✅ **Редактирование в ячейках** - клик на ячейку для редактирования  
✅ **Автосохранение** - данные сохраняются при выходе из ячейки  
✅ **Фильтры по колонкам** - в заголовке каждой колонки  
✅ **Сортировка** - клик на заголовок колонки  
✅ **Добавление строк** - кнопка "+ Добавить гостя"  
✅ **Удаление строк** - кнопка в каждой строке  
✅ **Выпадающий список статусов** - удобный выбор статуса регистрации  
✅ **Экспорт в CSV** - для бэкапа или обработки в Excel  
✅ **Статистика** - счетчики по статусам в реальном времени  
✅ **Изменяемые размеры колонок** - перетаскивание границ  

## Структура файлов

```
event/
├── views_guests_table.py           # View для таблицы и API endpoints
├── urls_guests_table.py            # URL маршруты
├── admin_guests_table_mixin.py     # Mixin для подключения к admin
└── GUESTS_TABLE_README.md          # Эта инструкция

templates/admin/event/
└── guests_table.html               # HTML шаблон с Tabulator.js
```

## Подключение

### Шаг 1: Добавить mixin в ModuleInstanceAdmin

В файле `event/admin.py` найдите класс `ModuleInstanceAdmin` и добавьте миксин:

```python
# В начале файла добавьте импорт
from .admin_guests_table_mixin import GuestsTableMixin

# Найдите класс ModuleInstanceAdmin и добавьте миксин первым
class ModuleInstanceAdmin(GuestsTableMixin, BaseAdminPage, ExportActionModelAdmin):
    # ... остальной код остается без изменений
```

**ВАЖНО:** `GuestsTableMixin` должен быть первым в списке наследования!

### Шаг 2: Готово!

Всё! Теперь в форме редактирования мероприятия появится раздел "Управление гостями" с кнопкой "📊 Управление списком гостей".

## Использование

1. Откройте мероприятие в админке
2. Прокрутите до раздела "Управление гостями"
3. Нажмите кнопку "📊 Управление списком гостей"
4. Работайте с таблицей как с Google Sheets:
   - Клик на ячейку → редактирование
   - Выход из ячейки → автосохранение через 1 секунду
   - Используйте фильтры в заголовках
   - Сортируйте по любой колонке
   - Добавляйте и удаляйте строки

### Работа со справочниками (Компания, Категория, Тип гостя, Продюсер):

При клике на ячейку справочника:
- **Появится выпадающий список** с существующими значениями
- **Начните вводить** - список автоматически отфильтруется
- **Выберите существующее** - клик по элементу списка
- **Создайте новое** - клик на "➕ Создать..." или просто введите и нажмите Enter

**Примечание для Продюсеров:** Можно выбрать только из существующих пользователей с ролью "Продюсер". Новых продюсеров нужно создавать в разделе "Доступы" → "Пользователи".

### Открытие карточки человека:

В первой колонке таблицы есть иконка 👤 - клик по ней откроет полную карточку контакта в новой вкладке, где можно:
- Просмотреть всю информацию о человеке
- Добавить фото
- Управлять соцсетями
- Посмотреть историю участия в мероприятиях

### Индикаторы сохранения:

- 💾 **"Сохранение..."** (желтый) - данные отправляются на сервер
- ✅ **"Сохранено"** (зеленый) - данные успешно сохранены
- ❌ **"Ошибка"** (красный) - произошла ошибка, попробуйте еще раз

## API Endpoints

Модуль создает следующие endpoints:

- `GET /admin/event/moduleinstance/<event_id>/guests-table/` - страница с таблицей
- `GET /admin/event/moduleinstance/<event_id>/guests-data/` - JSON данные гостей
- `POST /admin/event/moduleinstance/<event_id>/guest-save/` - сохранение гостя
- `POST /admin/event/moduleinstance/<event_id>/guest-delete/` - удаление гостя

## Технологии

- **Backend:** Django views с JSON API
- **Frontend:** [Tabulator.js](https://tabulator.info/) v5.5
- **Стилизация:** Custom CSS в стиле Django admin

## Удаление модуля

Если нужно отключить функциональность:

1. Удалите `GuestsTableMixin` из `ModuleInstanceAdmin` в `admin.py`
2. (Опционально) Удалите файлы:
   - `event/views_guests_table.py`
   - `event/urls_guests_table.py`
   - `event/admin_guests_table_mixin.py`
   - `templates/admin/event/guests_table.html`

## Кастомизация

### Изменить колонки таблицы

Отредактируйте `templates/admin/event/guests_table.html`, секцию `columns:` в JavaScript.

### Добавить валидацию

В файле `event/views_guests_table.py`, функция `guest_save_api`, добавьте проверки перед сохранением.

### Изменить стили

В `templates/admin/event/guests_table.html` найдите блок `<style>` и измените CSS.

## Примечания

- Все изменения сохраняются в базу данных немедленно при выходе из ячейки
- При создании нового контакта автоматически создаются связанные справочники (Компания, Категория, Тип гостя)
- Удаление записи из таблицы удаляет только Action, но не Contact
- Экспорт CSV использует разделитель ";" для совместимости с Excel

## Поддержка

При возникновении проблем проверьте:
1. Правильно ли подключен миксин в admin.py (должен быть первым)
2. Существует ли директория `templates/admin/event/`
3. Нет ли ошибок в консоли браузера (F12)
4. Доступны ли URL endpoints (проверьте в Django admin)

